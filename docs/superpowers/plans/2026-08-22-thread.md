# スレッド — 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** ノートを押すとカラムの中でスレッド（祖先 → 選択したイベント → 直接の返信）が開き、戻れるようにする。

**Architecture:** 根への `#e` 1 購読でスレッド全体を取り、純関数 `threadSpine` が背骨を計算する。スタックは `DeckColumn` のコンポーネント状態で、カラム 1 本を範囲とする context 経由でノートから push する。`ColumnDef` も `ColumnSource` も変えない。

**Tech Stack:** TypeScript / SolidJS / Vitest / Playwright（ADR-0020: Nostr 用ライブラリは使わない）

**Spec:** [docs/superpowers/specs/2026-08-22-thread-design.md](../specs/2026-08-22-thread-design.md)

## Global Constraints

- **ADR-0004**: kind 固有の知識は kind 側に置き、共有層は kind を知らない。判定基準は「置こうとしているものが『この kind のこのタグは何を意味するか』を含むか」
- **ADR-0011**: 劣化を隠さない。祖先の連鎖が切れたことを黙らせない
- **ADR-0020**: Nostr 用ライブラリを使わない
- **ADR-0026**: 押しても何も起きないものを押せる見た目にしない
- **ADR-0029**: `src/core/nostr/build/` と `src/core/write/` はミューテーションテストで守る。**本計画が触るファイルはその範囲外**なので `pnpm mutation` の対象にならない
- コメントは非自明な WHY だけ。WHAT・変更履歴・タスク ID・レビュー番号は書かない
- テストは**捕まえる変異を各テストに書き、その変異を入れて実際に落ちることを確認してから**次へ進む
- 型検査は `pnpm typecheck`（= `tsc -b`）。**`tsc --noEmit` はこのリポジトリでは何も検査しない**（ルート tsconfig が `files: []` + project references）
- 整形は `pnpm exec biome check --write <files>`、テストは `pnpm vitest run <file>`

---

## ファイル構成

| ファイル | 責務 |
|---|---|
| `src/core/nostr/event-refs.ts` | `threadRoot()` を追加（`replyTarget` の隣） |
| `src/core/view/thread-spine.ts` | 背骨を計算する純関数（新規） |
| `src/core/read/source.ts` | `Order` から `"thread-tree"` を削除 |
| `src/core/read/section-reader.ts` | `"thread-tree"` に言及したコメントを削除 |
| `src/routes/v1/thread-nav.ts` | カラム 1 本を範囲とする context（新規） |
| `src/routes/v1/ThreadView.tsx` | 背骨を描く（新規） |
| `src/routes/v1/DeckColumn.tsx` | スタック、スレッドのソース、ヘッダーの切り替え |
| `src/routes/v1/renderers/Note.tsx` | `<article>` の click ハンドラ |
| `e2e/fixtures/seed-thread.ts` | スレッドのシード（新規） |
| `e2e/thread.spec.ts` | e2e 1 本（新規） |

タスク 1〜3 がコア（互いに独立）、4〜7 が UI、8 が e2e。

---

## Task 1: `threadRoot()`

**Files:**
- Modify: `src/core/nostr/event-refs.ts`（`replyTarget` の直後）
- Test: `src/core/nostr/event-refs.test.ts`（既存に追記）

**Interfaces:**
- Consumes: 既存の `IdRef` / `idRef()`（同ファイル内、module-local）
- Produces: `threadRoot(event: NostrEvent): IdRef | undefined`

### なぜ `replyTarget` では足りないか

`replyTarget` は `reply` マーカーがあればそれを返し、無いときだけ `root` へフォールバックする。**返信への返信では根が取れない。**スレッドの購読は根に投げるので、根だけを返す関数が別に要る。

- [ ] **Step 1: 失敗するテストを書く**

`src/core/nostr/event-refs.test.ts` の末尾に足す。既存ファイルの先頭にある `evt` 相当のヘルパを読んで、その形に合わせること（無ければ下のものを定義する）。

```ts
describe("threadRoot", () => {
  const withTags = (tags: string[][]): NostrEvent =>
    ({
      id: "a".repeat(64),
      pubkey: "b".repeat(64),
      created_at: 1_700_000_000,
      kind: 1,
      tags,
      content: "",
      sig: "c".repeat(128),
    }) as NostrEvent;

  it("root マーカーの e タグを返す", () => {
    // 捕まえる変異: マーカーを見ずに最初の e タグを返す
    expect(
      threadRoot(
        withTags([
          ["e", "1".repeat(64), "", "reply", "9".repeat(64)],
          ["e", "2".repeat(64), "wss://r.example", "root", "8".repeat(64)],
        ]),
      ),
    ).toEqual({
      form: "id",
      id: "2".repeat(64),
      relay: "wss://r.example",
      pubkey: "8".repeat(64),
    });
  });

  it("reply マーカーに引きずられない", () => {
    // 捕まえる変異: replyTarget と同じ「reply があればそれ」の分岐を書く。
    // これを見逃すと、返信への返信で根ではなく親へ購読を張ることになり、
    // 祖先が 1 段しか取れない。
    expect(
      threadRoot(withTags([["e", "1".repeat(64), "", "reply", "9".repeat(64)]])),
    ).toBeUndefined();
  });

  it("マーカー無しの e タグを根と誤認しない", () => {
    // 捕まえる変異: tag[3] を見ずに e タグを拾う。NIP-10 の位置ベースの
    // 旧形式は deprecated で、NIP-10 自身が「曖昧で解決不能」としている。
    expect(threadRoot(withTags([["e", "1".repeat(64)]]))).toBeUndefined();
  });

  it("root マーカーが無ければ undefined（自分が根）", () => {
    // 捕まえる変異: 見つからないときに自分の id を返す。呼び出し側が
    // 「自分が根」を判定できなくなる。
    expect(threadRoot(withTags([]))).toBeUndefined();
  });

  it("id が 64 桁 hex でなければ落とす", () => {
    // 捕まえる変異: idRef を通さず生の値を返す
    expect(threadRoot(withTags([["e", "zz", "", "root"]]))).toBeUndefined();
  });
});
```

- [ ] **Step 2: 落ちることを確認する**

Run: `pnpm vitest run src/core/nostr/event-refs.test.ts`
Expected: FAIL（`threadRoot is not defined`）

- [ ] **Step 3: 実装する**

`replyTarget` の直後に足す。

```ts
/**
 * スレッドの根を返す。**`root` マーカーの付いた `e` タグだけを見る。**
 *
 * `replyTarget` は `reply` があればそちらを優先するので、返信への返信では
 * 根が取れない。スレッドの購読は根に投げる（NIP-10 を守る返信は深さに
 * 関わらず全員が根を指すため）ので、根だけを返す経路が別に要る。
 *
 * 見つからなければ `undefined`。**自分の id を返さない** —— 呼び出し側が
 * 「このイベント自身が根である」ことを判定できなくなる。
 */
export const threadRoot = (event: NostrEvent): IdRef | undefined => {
  for (const tag of event.tags) {
    if (tag[0] !== "e" || tag[3] !== "root") continue;
    const ref = idRef(tag[1] ?? "", tag[2], tag[4]);
    if (ref) return ref;
  }
  return undefined;
};
```

- [ ] **Step 4: 通ることを確認する**

Run: `pnpm vitest run src/core/nostr/event-refs.test.ts`
Expected: PASS

- [ ] **Step 5: 各テストの「捕まえる変異」を実際に入れて落ちることを確認する**

5 件それぞれについて、コメントに書いた変異を `event-refs.ts` に入れて**そのテストが落ちること**を目で確認してから戻す。落ちなければテストが弱いので、落ちるまで主張を強くする。

- [ ] **Step 6: 整形・全体テスト・Commit**

```bash
pnpm exec biome check --write src/core/nostr/event-refs.ts src/core/nostr/event-refs.test.ts
pnpm typecheck && pnpm vitest run
git add src/core/nostr/event-refs.ts src/core/nostr/event-refs.test.ts
git commit -m "feat(nostr): threadRoot (NIP-10 の root マーカー)"
```

---

## Task 2: `threadSpine()`

**Files:**
- Create: `src/core/view/thread-spine.ts`
- Test: `src/core/view/thread-spine.test.ts`

**Interfaces:**
- Consumes: `replyTarget`（`src/core/nostr/event-refs.ts`、既存）、`compareEvents`（`src/core/read/sorted-events.ts`、既存）
- Produces:

```ts
export type ThreadSpine = {
  ancestors: NostrEvent[];  // 根に近い順。focus を含まない
  focus: NostrEvent | undefined;
  replies: NostrEvent[];    // created_at 昇順
  reachedRoot: boolean;
};
export const threadSpine: (
  events: readonly NostrEvent[],
  focusId: string,
) => ThreadSpine;
```

- [ ] **Step 1: 失敗するテストを書く**

```ts
// src/core/view/thread-spine.test.ts
import { describe, expect, it } from "vitest";
import type { NostrEvent } from "../nostr/event";
import { threadSpine } from "./thread-spine";

/** `id` の 1 文字目だけで区別する読みやすい偽イベント。 */
const note = (
  key: string,
  options?: { root?: string; reply?: string; at?: number },
): NostrEvent => {
  const tags: string[][] = [];
  if (options?.root) tags.push(["e", options.root.repeat(64), "", "root"]);
  if (options?.reply) tags.push(["e", options.reply.repeat(64), "", "reply"]);
  return {
    id: key.repeat(64),
    pubkey: "b".repeat(64),
    created_at: options?.at ?? 1_700_000_000,
    kind: 1,
    tags,
    content: key,
    sig: "c".repeat(128),
  } as NostrEvent;
};
const id = (key: string) => key.repeat(64);

describe("threadSpine", () => {
  it("祖先を根に近い順で並べる", () => {
    // 捕まえる変異: replyTarget ではなく threadRoot で登る —— 中間の
    // 祖先を飛ばして根へ跳ぶので、ancestors が 1 件になる。
    const root = note("1");
    const mid = note("2", { root: "1", reply: "1" });
    const focus = note("3", { root: "1", reply: "2" });
    const spine = threadSpine([focus, mid, root], id("3"));
    expect(spine.ancestors.map((e) => e.content)).toEqual(["1", "2"]);
    expect(spine.reachedRoot).toBe(true);
  });

  it("focus 自身が根なら祖先は空で reachedRoot は true", () => {
    // 捕まえる変異: 祖先が空のとき reachedRoot を false にする ——
    // 根を開いただけで「連鎖が切れている」と表示される。
    const root = note("1");
    const spine = threadSpine([root], id("1"));
    expect(spine.ancestors).toEqual([]);
    expect(spine.reachedRoot).toBe(true);
  });

  it("途中の祖先が欠けていれば reachedRoot は false", () => {
    // 捕まえる変異: 常に true を返す —— 祖先が欠けたスレッドが
    // 「根から始まっている」ように見え、誰が誰に返信したのかを読み違える。
    const focus = note("3", { root: "1", reply: "2" });
    const spine = threadSpine([focus], id("3"));
    expect(spine.ancestors).toEqual([]);
    expect(spine.reachedRoot).toBe(false);
  });

  it("replies は focus を直接の親とするものだけ", () => {
    // 捕まえる変異: 根を指す全イベントを replies に入れる —— 孫や
    // 別の枝が混ざり、1 節が決めた「背骨だけ」が壊れる。
    const root = note("1");
    const focus = note("2", { root: "1", reply: "1" });
    const child = note("3", { root: "1", reply: "2" });
    const grandchild = note("4", { root: "1", reply: "3" });
    const sibling = note("5", { root: "1", reply: "1" });
    const spine = threadSpine(
      [root, focus, child, grandchild, sibling],
      id("2"),
    );
    expect(spine.replies.map((e) => e.content)).toEqual(["3"]);
  });

  it("replies は created_at 昇順", () => {
    // 捕まえる変異: 降順にする / 並べ替えない
    const focus = note("1");
    const later = note("3", { reply: "1", at: 1_700_000_200 });
    const earlier = note("2", { reply: "1", at: 1_700_000_100 });
    const spine = threadSpine([focus, later, earlier], id("1"));
    expect(spine.replies.map((e) => e.content)).toEqual(["2", "3"]);
  });

  it("created_at が同値なら id 昇順", () => {
    // 捕まえる変異: tie-break を持たない —— 入力順に依存して並びが
    // 揺れ、リレーの配送順で表示が変わる。
    const focus = note("1");
    const b = note("3", { reply: "1", at: 1_700_000_100 });
    const a = note("2", { reply: "1", at: 1_700_000_100 });
    const spine = threadSpine([focus, b, a], id("1"));
    expect(spine.replies.map((e) => e.content)).toEqual(["2", "3"]);
  });

  it("自分自身を親に指すイベントで無限ループしない", () => {
    // 捕まえる変異: 訪問済み集合を持たない。リレーは NIP-10 のタグ意味論を
    // 検証しないので、この形のイベントは publish できてしまう。
    const loop = note("1", { reply: "1" });
    const spine = threadSpine([loop], id("1"));
    expect(spine.ancestors).toEqual([]);
    expect(spine.reachedRoot).toBe(false);
  });

  it("祖先どうしが循環していても止まる", () => {
    // 捕まえる変異: 上と同じ。2 件で輪を作る形。
    const a = note("1", { reply: "2" });
    const b = note("2", { reply: "1" });
    const spine = threadSpine([a, b], id("1"));
    expect(spine.ancestors.length).toBeLessThanOrEqual(1);
    expect(spine.reachedRoot).toBe(false);
  });

  it("focus が events に無ければ focus は undefined", () => {
    // 捕まえる変異: 例外を投げる —— まだ届いていないだけの id で
    // カラムごと落ちる。
    const spine = threadSpine([note("1")], id("9"));
    expect(spine.focus).toBeUndefined();
    expect(spine.ancestors).toEqual([]);
    expect(spine.replies).toEqual([]);
  });
});
```

- [ ] **Step 2: 落ちることを確認する**

Run: `pnpm vitest run src/core/view/thread-spine.test.ts`
Expected: FAIL（`Cannot find module './thread-spine'`）

- [ ] **Step 3: 実装する**

```ts
// src/core/view/thread-spine.ts
import { replyTarget } from "../nostr/event-refs";
import type { NostrEvent } from "../nostr/event";
import { compareEvents } from "../read/sorted-events";

export type ThreadSpine = {
  /** 根に近い順。`focus` は含まない。 */
  ancestors: NostrEvent[];
  focus: NostrEvent | undefined;
  /** `created_at` 昇順。`focus` を直接の親とするものだけ。 */
  replies: NostrEvent[];
  /**
   * 祖先の連鎖が根まで到達したか。**`false` を黙らせないこと** ——
   * 途中の祖先が欠けたスレッドは「根から始まっている」ように見え、
   * 誰が誰に返信したのかを読み違える (ADR-0011)。
   */
  reachedRoot: boolean;
};

/**
 * 表示する 1 本の背骨を計算する。木ではない —— 兄弟の枝も返信の返信も
 * 出さない (仕様 1 節)。ネットワークも store も触らない。
 */
export const threadSpine = (
  events: readonly NostrEvent[],
  focusId: string,
): ThreadSpine => {
  const byId = new Map(events.map((event) => [event.id, event]));
  const focus = byId.get(focusId);
  if (!focus) {
    return { ancestors: [], focus: undefined, replies: [], reachedRoot: false };
  }

  // 上へ登る。**訪問済みを持つ** —— 壊れた (あるいは悪意ある) イベントは
  // 自分自身や祖先を親として指せる。リレーは NIP-10 のタグ意味論を検証
  // しないので、この形は publish できてしまう。
  const ancestors: NostrEvent[] = [];
  const seen = new Set<string>([focus.id]);
  let cursor = focus;
  let reachedRoot = true;
  for (;;) {
    const parentRef = replyTarget(cursor);
    if (!parentRef) break;
    if (seen.has(parentRef.id)) {
      reachedRoot = false;
      break;
    }
    const parent = byId.get(parentRef.id);
    if (!parent) {
      reachedRoot = false;
      break;
    }
    seen.add(parent.id);
    ancestors.push(parent);
    cursor = parent;
  }
  ancestors.reverse();

  const replies = events
    .filter((event) => replyTarget(event)?.id === focusId)
    .sort((a, b) => a.created_at - b.created_at || compareEvents(a, b));

  return { ancestors, focus, replies, reachedRoot };
};
```

**注意:** `compareEvents` は `created_at` **降順**の全順序である（`sorted-events.ts`）。ここでは `created_at` の比較を昇順で先に行い、同値のときだけ `compareEvents` の tie-break（`id` 昇順）を使う —— `section-reader.ts` の `#displayOrdered` と同じ手筋。`compareEvents` をそのまま使うと降順になる。実装前に `sorted-events.ts` を読んで tie-break の向きを確認すること。

- [ ] **Step 4: 通ることを確認する**

Run: `pnpm vitest run src/core/view/thread-spine.test.ts`
Expected: PASS（9 件）

- [ ] **Step 5: 「捕まえる変異」を 9 件とも確認する**

特に「祖先を根に近い順で並べる」で `replyTarget` を `threadRoot` に置き換えたとき、`ancestors` が 1 件になって落ちることを必ず見ること。

- [ ] **Step 6: 整形・全体テスト・Commit**

```bash
pnpm exec biome check --write src/core/view/thread-spine.ts src/core/view/thread-spine.test.ts
pnpm typecheck && pnpm vitest run
git add src/core/view/thread-spine.ts src/core/view/thread-spine.test.ts
git commit -m "feat(view): スレッドの背骨を計算する"
```

---

## Task 3: `Order` から `"thread-tree"` を削除する

**Files:**
- Modify: `src/core/read/source.ts:18`
- Modify: `src/core/read/section-reader.ts`（`#displayOrdered` の doc コメント内、`"thread-tree"` に言及した 1 行）

**Interfaces:**
- Produces: `Order = "created-at-desc" | "created-at-asc"`

### なぜ足さずに削るのか

背骨は**並べ替えではなく計算**である（Task 2）。順序関数はイベント 2 件を比べる形しか取れず、「focus からの距離」という文脈依存の値を持てない。予約したまま実装されない値は、次に読む人へ「ここに答えがある」と誤って知らせる。

- [ ] **Step 1: 参照が無いことを確認する**

```bash
grep -rn "thread-tree" src/ e2e/
```
Expected: `source.ts` の型定義と `section-reader.ts` のコメントの 2 箇所だけ。**他に参照があれば止まって報告すること**（計画の前提が崩れている）。

- [ ] **Step 2: 削除する**

`source.ts`:

```ts
export type Order = "created-at-desc" | "created-at-asc";
```

`section-reader.ts` の `#displayOrdered` の doc から次の 1 行を消す。

```
   * "thread-tree" はスレッドカラムの計画で足す。それまでは降順で扱う。
```

- [ ] **Step 3: 型検査と全体テスト**

Run: `pnpm typecheck && pnpm vitest run`
Expected: エラー 0、全件 PASS。**型エラーが出たら削除が早すぎる**（どこかが使っている）ので、Step 1 の grep を見直すこと。

- [ ] **Step 4: Commit**

```bash
pnpm exec biome check --write src/core/read/source.ts src/core/read/section-reader.ts
git add src/core/read/source.ts src/core/read/section-reader.ts
git commit -m "refactor(read): Order の thread-tree を削る"
```

---

## Task 4: カラム 1 本を範囲とする context

**Files:**
- Create: `src/routes/v1/thread-nav.ts`
- Test: `src/routes/v1/thread-nav.test.tsx`

**Interfaces:**
- Produces:

```ts
export const ThreadNavProvider: ParentComponent<{ open: (focusId: string) => void }>;
/** カラムの外では undefined。 */
export const useThreadNav: () => ((focusId: string) => void) | undefined;
```

### `useRender()` と違って投げない

`useRender()`（`src/core/view/render-context.tsx`）は provider が無ければ例外を投げる。そちらは「渡し忘れ＝常に配線ミス」だからである。こちらは違う —— `/debug/v1-section` のようにナビゲーションを持たない面でイベントを描くのは正当な使い方で、そこではノートが押せないだけでよい。**投げると、デバッグルートがスレッドと無関係に落ちる。**

- [ ] **Step 1: 失敗するテストを書く**

このリポジトリの Solid テストは `createRoot` を使う（testing-library は使わない）。既存の `src/core/view/render-context.test.tsx` を読んで同じ手筋に合わせること。

```tsx
// src/routes/v1/thread-nav.test.tsx
import { createRoot } from "solid-js";
import { describe, expect, it } from "vitest";
import { ThreadNavProvider, useThreadNav } from "./thread-nav";

describe("useThreadNav", () => {
  it("provider の外では undefined を返す", () => {
    // 捕まえる変異: useRender と同じく例外を投げる。投げると
    // /debug/v1-section のようにナビゲーションを持たない面が、
    // スレッドと無関係に落ちる。
    createRoot((dispose) => {
      expect(useThreadNav()).toBeUndefined();
      dispose();
    });
  });

  it("provider の中では渡された関数を返す", () => {
    // 捕まえる変異: 常に undefined を返す（ノートが永久に押せない）
    const opened: string[] = [];
    createRoot((dispose) => {
      const Child = () => {
        useThreadNav()?.("abc");
        return null;
      };
      // JSX を評価するだけで child が走る形にする。既存の
      // render-context.test.tsx の書き方に合わせること。
      <ThreadNavProvider open={(id) => opened.push(id)}>
        <Child />
      </ThreadNavProvider>;
      dispose();
    });
    expect(opened).toEqual(["abc"]);
  });
});
```

**注意:** 2 件目は JSX を評価するだけでは子が走らない書き方になりうる。`render-context.test.tsx` が provider の中身をどう起動しているかを読んで、**実際に子が実行される形**に直すこと。子が走らないまま `expect` が通ると、このテストは何も検証していない。

- [ ] **Step 2: 落ちることを確認する**

Run: `pnpm vitest run src/routes/v1/thread-nav.test.tsx`
Expected: FAIL（`Cannot find module './thread-nav'`）

- [ ] **Step 3: 実装する**

```tsx
// src/routes/v1/thread-nav.ts
import { type ParentComponent, createContext, useContext } from "solid-js";

type OpenThread = (focusId: string) => void;

const ThreadNavContext = createContext<OpenThread>();

export const ThreadNavProvider: ParentComponent<{ open: OpenThread }> = (
  props,
) => (
  <ThreadNavContext.Provider value={props.open}>
    {props.children}
  </ThreadNavContext.Provider>
);

/**
 * このノートを起点にスレッドを開く関数。**provider が無ければ
 * `undefined` を返し、例外を投げない。**
 *
 * `useRender()` が投げるのは「provider を渡し忘れた」が常に配線ミス
 * だからだが、こちらは違う —— `/debug/v1-section` のようにナビゲーションを
 * 持たない面でイベントを描くのは正当な使い方であり、そこではノートが
 * 押せないだけでよい。投げると、その面がスレッドと無関係に落ちる。
 */
export const useThreadNav = (): OpenThread | undefined =>
  useContext(ThreadNavContext);
```

**注意:** JSX を含むので拡張子は `.tsx`。ファイル構成表の `.ts` は誤りなので `.tsx` で作り、この計画の記述と食い違ったことを報告に書くこと。

- [ ] **Step 4: 通ることを確認する**

Run: `pnpm vitest run src/routes/v1/thread-nav.test.tsx`
Expected: PASS

- [ ] **Step 5: 「捕まえる変異」を 2 件とも確認する**

- [ ] **Step 6: 整形・全体テスト・Commit**

```bash
pnpm exec biome check --write src/routes/v1/thread-nav.tsx src/routes/v1/thread-nav.test.tsx
pnpm typecheck && pnpm vitest run
git add src/routes/v1/thread-nav.tsx src/routes/v1/thread-nav.test.tsx
git commit -m "feat(v1): カラム範囲のスレッドナビゲーション context"
```

---

## Task 5: ノートを押せるようにする

**Files:**
- Modify: `src/routes/v1/renderers/Note.tsx`（`NoteFull` の `<article>` と `NoteCompact` の `<article>`）
- Test: `src/routes/v1/renderers/Note.test.tsx`（既存に追記）

**Interfaces:**
- Consumes: `useThreadNav()`（Task 4）

### 規則

`<article data-testid="note">` を押すと、**その `<article>` が描いているイベントの**スレッドが開く。`NoteFull` と `NoteCompact` の両方。`variant` で区別しない。

入れ子は内側が勝つ。内側の `<article>` が `stopPropagation()` するので外側へ届かない。引用先も返信先もこれで開ける。

- [ ] **Step 1: 失敗するテストを書く**

既存の `Note.test.tsx` の書き方（`createRoot` + `RenderProvider`）に合わせること。

```tsx
describe("スレッドを開く", () => {
  it("ノートを押すとそのイベントの id で開く", () => {
    // 捕まえる変異: 押せるようにしない / 別の id を渡す
  });

  it("入れ子のノートを押すと内側の id で開き、外側へ伝播しない", () => {
    // 捕まえる変異: stopPropagation を落とす —— 引用先を押したのに
    // 外側のノートのスレッドが開く（あるいは 2 回開く）。
  });

  it("名前のホバートリガーを押してもスレッドは開かない", () => {
    // 捕まえる変異: 対話要素の判定を落とす —— 名前を押すと
    // ホバーカードではなくスレッドが開く。
  });

  it("useThreadNav が undefined なら押せる見た目を持たない", () => {
    // 捕まえる変異: 常に cursor-pointer と onClick を付ける。
    // ADR-0026「押しても何も起きないものを押せる見た目にしない」。
  });

  it("ドラッグでテキストを選択した後は開かない", () => {
    // 捕まえる変異: mousedown の座標を覚えない —— 本文を選択しようと
    // するたびにスレッドが開き、コピーができない。
  });
});
```

**テスト本体は実装者が書く。**上のコメント（捕まえる変異）が主張の定義である。`Note.test.tsx` が既にどうやって DOM を組み立てて click を送っているかを読み、同じ手筋を使うこと。DOM イベントの発火には `element.dispatchEvent(new MouseEvent("click", { bubbles: true }))` を使う（`bubbles: true` が無いと入れ子の伝播が試せない）。

- [ ] **Step 2: 落ちることを確認する**

Run: `pnpm vitest run src/routes/v1/renderers/Note.test.tsx`
Expected: FAIL

- [ ] **Step 3: 実装する**

`Note.tsx` に、両方の `<article>` が使う小さなヘルパを置く。

```tsx
/**
 * `<article>` の click を「このイベントのスレッドを開く」に変える。
 *
 * **入れ子は内側が勝つ。**引用カードや返信先の中のノートを押したら、
 * 内側の `<article>` がここで `stopPropagation()` するので外側へ届かない。
 * 深さに関係なく同じ規則で効く。
 *
 * 対話要素の上では発火しない —— リンク・名前・アクション列はそれぞれ
 * 自分の目的地を持つ。`closest()` で祖先まで見るのは、押されたのが
 * `<button>` の中の `<span>` でも同じ判定になるようにするため。
 *
 * ドラッグで本文を選択した後も発火しない。`mousedown` と `mouseup` の
 * 座標が動いていたら選択の意図とみなす —— そうしないと本文をコピー
 * しようとするたびにスレッドが開く。
 */
const useOpenThreadOnClick = (event: () => NostrEvent) => {
  const open = useThreadNav();
  let downAt: { x: number; y: number } | undefined;

  const isInteractive = (target: EventTarget | null) =>
    target instanceof Element &&
    target.closest("a, button, [role='button'], input, textarea") !== null;

  return () =>
    open === undefined
      ? {}
      : {
          class: "cursor-pointer",
          onMouseDown: (e: MouseEvent) => {
            downAt = { x: e.clientX, y: e.clientY };
          },
          onClick: (e: MouseEvent) => {
            if (isInteractive(e.target)) return;
            const moved =
              downAt !== undefined &&
              (Math.abs(e.clientX - downAt.x) > DRAG_SLOP ||
                Math.abs(e.clientY - downAt.y) > DRAG_SLOP);
            if (moved) return;
            e.stopPropagation();
            open(event().id);
          },
        };
};

/** これ以上動いたら「押した」ではなく「選択した」とみなす。 */
const DRAG_SLOP = 4;
```

`NoteFull` / `NoteCompact` の `<article>` にこれを展開する。既存の `class` と衝突するので、`classList` か文字列連結でまとめること（`class` を 2 回書くと後勝ちで既存のスタイルが消える）。

**`useThreadNav()` が `undefined` のときは `{}` を返す** —— `onClick` も `cursor-pointer` も付かない（ADR-0026）。

- [ ] **Step 4: 通ることを確認する**

Run: `pnpm vitest run src/routes/v1/renderers/Note.test.tsx`
Expected: PASS

- [ ] **Step 5: 「捕まえる変異」を 5 件とも確認する**

- [ ] **Step 6: 整形・全体テスト・Commit**

```bash
pnpm exec biome check --write src/routes/v1/renderers/Note.tsx src/routes/v1/renderers/Note.test.tsx
pnpm typecheck && pnpm vitest run
git add src/routes/v1/renderers/Note.tsx src/routes/v1/renderers/Note.test.tsx
git commit -m "feat(v1): ノートを押してスレッドを開く"
```

---

## Task 6: `ThreadView`

**Files:**
- Create: `src/routes/v1/ThreadView.tsx`

**Interfaces:**
- Consumes: `threadSpine`（Task 2）、既存の `EventView`（`src/routes/v1/EventView.tsx`）
- Produces:

```tsx
const ThreadView: Component<{
  events: () => NostrEvent[];
  focusId: string;
}>;
```

### 描く形

```
祖先（compact、根に近い順）
選択したイベント（full）
返信（compact、created_at 昇順）
```

祖先の連鎖が切れていれば（`reachedRoot === false`）、祖先の**上**にそれを出す。黙って根から始まっているように見せない（ADR-0011）。

`focus` が `undefined` なら「読み込み中」。

- [ ] **Step 1: 実装する**

`ColumnItems.tsx` の `<ul data-testid="items" class="divide-y">` と同じ区切りの手筋に合わせる。`content-visibility` の最適化はスレッドでは要らない（件数が桁違いに少ない）。

```tsx
const ThreadView: Component<{
  events: () => NostrEvent[];
  focusId: string;
}> = (props) => {
  const spine = createMemo(() => threadSpine(props.events(), props.focusId));

  return (
    <ul data-testid="thread" class="divide-y">
      <Show when={!spine().reachedRoot}>
        {/*
          黙って根から始まっているように見せない (ADR-0011)。祖先が
          欠けたスレッドは「誰が誰に返信したのか」を読み違えさせる。
        */}
        <li data-testid="thread-truncated" class="c-secondary p-3 text-caption">
          これより前の返信は取得できていません
        </li>
      </Show>
      <For each={spine().ancestors}>
        {(event) => (
          <li data-testid="thread-ancestor">
            <EventView id={event.id} variant="compact" />
          </li>
        )}
      </For>
      <Show
        when={spine().focus}
        fallback={
          <li data-testid="thread-focus" class="c-secondary p-3 text-caption">
            読み込み中
          </li>
        }
      >
        {(focus) => (
          <li data-testid="thread-focus">
            <EventView id={focus().id} variant="full" />
          </li>
        )}
      </Show>
      <For each={spine().replies}>
        {(event) => (
          <li data-testid="thread-reply">
            <EventView id={event.id} variant="compact" />
          </li>
        )}
      </For>
    </ul>
  );
};

export default ThreadView;
```

**注意:** `EventView` に id で渡すのは、`compact` の入れ子・欠落時の「読み込み中」・レンダラの選択を既存の 1 箇所に任せるため。`spine()` が持っているイベントを直接レンダラへ渡すと、その 3 つを `ThreadView` が再実装することになる。

- [ ] **Step 2: 型検査**

Run: `pnpm typecheck`
Expected: エラー 0

- [ ] **Step 3: Commit**

このタスクに単体テストは書かない。`threadSpine` の主張は Task 2 が全部持っており、ここに残るのは「背骨を上から順に `<li>` に落とす」だけで、それを検証するテストは実装と同じ形を書き写すことになる。**動くかどうかは Task 8 の e2e が測る。**

```bash
pnpm exec biome check --write src/routes/v1/ThreadView.tsx
pnpm typecheck && pnpm vitest run
git add src/routes/v1/ThreadView.tsx
git commit -m "feat(v1): スレッドの描画"
```

---

## Task 7: `DeckColumn` にスタックを載せる

**Files:**
- Modify: `src/routes/v1/DeckColumn.tsx`

**Interfaces:**
- Consumes: `threadRoot`（Task 1）、`ThreadNavProvider`（Task 4）、`ThreadView`（Task 6）、既存の `createSection`

- [ ] **Step 1: スタックとスレッドのソースを足す**

```tsx
/** 空 = 根のカラム。push でスレッドへ進み、pop で戻る。 */
const [stack, setStack] = createSignal<string[]>([]);
const focusId = () => stack().at(-1);

/**
 * スレッドの購読は**根**に投げる。NIP-10 を守る返信は深さに関わらず
 * 全員が根を `e` タグで指すので、これ 1 本で祖先も返信も届く。
 *
 * **根の id でメモ化する。** `createSection` は `createEffect` の中で
 * `source()` を読んで `SectionReader` を作り直すので、内容が同じでも
 * 毎回新しいオブジェクトを返すと、スレッド内で 1 段進むたびに購読が
 * 張り直されて items が空から積み直しになる。
 */
const threadRootId = createMemo(() => {
  const id = focusId();
  if (!id) return undefined;
  const focus = props.store.get(id);
  if (!focus) return id;
  return threadRoot(focus)?.id ?? id;
});
```

**`store` は `useRender().store` から取る。** `DeckColumn` は `store` を props で受け取っておらず、`v1.tsx:775` の `RenderProvider` の中にある（`v1.tsx:792` で `<DeckColumn>` を描いている）。props を 1 つ増やすより context から取るほうが、既存の `ColumnItems` / `EventView` と同じ経路になる。

- [ ] **Step 2: スレッドのセクションを作る**

```tsx
const threadSection = createSection({
  source: createMemo(() => {
    const root = threadRootId();
    // 根が決まっていないときは誰にもマッチしないフィルタを渡す。
    // 空配列を渡すと NIP-01 では「無制限」になる (resolve-source.ts の
    // followees と同じ罠)。
    if (!root) return { type: "nostr" as const, filters: [] };
    return {
      type: "nostr" as const,
      filters: [{ ids: [root] }, { kinds: [1], "#e": [root] }],
      ...(RELAYS_OVERRIDE ? { relays: RELAYS_OVERRIDE } : {}),
    };
  }),
  manager: props.manager,
});
```

**`filters: []` は「何も購読しない」で安全である**（計画作成時に確認済み）。`planQuery`（`src/core/read/query-plan.ts:52`）は `for (const filter of filters)` でリレー割り当てを組み立てるので、フィルタが 0 本なら `perRelay` が空のままになり、リレーが 1 本も選ばれない。

**空配列を渡してよいのはここまでで、`authors: []` とは別物である。**`resolve-source.ts` が警告しているのは「`{ kinds: [1] }` のように `authors` を落とすと NIP-01 では『誰の投稿でもよい』になる」という話で、こちらは**フィルタ自体が 0 本**なので該当しない。`{ }`（空のフィルタオブジェクト）を 1 本渡すのは無制限購読になるので、**絶対にそちらへ変えないこと。**

- [ ] **Step 3: 描画を切り替える**

`ThreadNavProvider` でカラムの中身全体を包み、`open` で push する。本文は `focusId()` の有無で `ThreadView` と `ColumnItems` を切り替える。**根のカラムの `createSection` は破棄しない**（戻ったときに取り直しが起きないため）。

ヘッダーは `focusId()` があるとき、タイトルを「スレッド」にし、左端の「カラムを左へ」ボタンを戻るボタン（`setStack((s) => s.slice(0, -1))`）に差し替える。`data-testid="thread-back"` を付ける。

- [ ] **Step 4: 型検査と全体テスト**

Run: `pnpm typecheck && pnpm vitest run`
Expected: エラー 0、全件 PASS

- [ ] **Step 5: 手で動かして確かめる**

`pnpm dev` を起動し、`/v1?relays=...` でログインしてノートを押す。次を目で見る。

1. スレッドが開き、祖先と返信が出る
2. 戻るボタンで元のカラムに戻り、**スクロール位置と items が保たれている**（取り直しが起きていない）
3. スレッド内の祖先を押すと、その祖先を選択したイベントとする背骨に引き直される
4. **そのとき購読が張り直されていない**（開発者モードの `phase` が `initial` に戻らない）

4 が崩れていたら Step 1 のメモ化が効いていない。

- [ ] **Step 6: 整形・Commit**

```bash
pnpm exec biome check --write src/routes/v1/DeckColumn.tsx
git add src/routes/v1/DeckColumn.tsx
git commit -m "feat(v1): カラムの中にスレッドを積む"
```

---

## Task 8: e2e

**Files:**
- Create: `e2e/fixtures/seed-thread.ts`
- Create: `e2e/thread.spec.ts`
- Modify: `e2e/global-setup.ts`（シードを 1 行足す）

**Interfaces:**
- Consumes: 既存の `seedLocalRelay`（`e2e/fixtures/seed.js`）

- [ ] **Step 1: シードを作る**

既存の `e2e/fixtures/seed-preview.ts` を読んで、鍵の作り方・署名・publish の手筋をそのまま踏襲する。**pubkey は既存フィクスチャと衝突しないものにすること**（`e2e/fixtures/fixture-pubkeys.test.ts` が衝突を機械的に検出する）。

置くイベント:

| 役 | タグ |
|---|---|
| 根 | 無し |
| 中間 | `["e", 根, "", "root"]` |
| 選択するイベント | `["e", 根, "", "root"]`, `["e", 中間, "", "reply"]` |
| 返信 1 | `["e", 根, "", "root"]`, `["e", 選択, "", "reply"]` |
| 返信 2 | 同上（`created_at` は返信 1 より後） |

**祖先は必ず 2 段にする。**1 段だと `replyTarget` と `threadRoot` の取り違えが通ってしまう。

- [ ] **Step 2: e2e を書く**

```ts
test("ノートを押すとスレッドが開き、入れ子からも辿れて、戻れる", async ({ page }) => {
  // 1. ホームのノートを押す
  // 2. thread-ancestor が 2 件、thread-focus が選択したイベント、
  //    thread-reply が 2 件、順序どおりに出る
  // 3. thread-ancestor の 1 件目 (根) を押す
  //    → 背骨が引き直され、thread-ancestor が 0 件・thread-reply が
  //      中間 1 件になる
  //    捕まえる変異: Note.tsx の stopPropagation を落とす / compact に
  //    ハンドラを付けない —— どちらでもこの遷移が起きない
  // 4. thread-back を押す → 元のカラムへ戻り、items が出ている
});
```

**テスト本体は実装者が書く。**`e2e/v1.spec.ts` の `stubSigner` とデッキの `addInitScript` の手筋をそのまま使うこと。

- [ ] **Step 3: 走らせる**

Run: `pnpm exec playwright test e2e/thread.spec.ts`
Expected: PASS

- [ ] **Step 4: 変異を確認する**

`Note.tsx` の `stopPropagation()` を消して e2e を走らせ、**手順 3 が落ちること**を確認してから戻す。

- [ ] **Step 5: 全体を走らせる**

```bash
pnpm typecheck && pnpm vitest run && pnpm exec playwright test
```
Expected: 全件 PASS

- [ ] **Step 6: Commit**

```bash
pnpm exec biome check --write e2e/
git add e2e/
git commit -m "test(e2e): スレッドを開いて辿って戻る"
```

---

## Task 9: 記録の更新

**Files:**
- Modify: `docs/design/v1-feature-inventory.md`（2.2 節）
- Modify: `docs/design/read-layer-followups.md`

- [ ] **Step 1: 棚卸しの 2.2 を消化済みにする**

`Order` の `"thread-tree"` に言及した記述（「`section-reader.ts:223` が『スレッドカラムの計画で足す』と書いたまま止まっている」）は**削って**、削除したことを書く。残るのは「返信を書くこと」と「兄弟の枝・返信の返信を同時に見せること」。

- [ ] **Step 2: followups にスライスの記録を足す**

最低限、次の 3 つを記録する。

- `Order` に予約されていた `"thread-tree"` が**答えではなかった**こと。背骨は並べ替えではなく計算で、順序関数は「focus からの距離」という文脈依存の値を持てない
- スレッドのソースを**根の id でメモ化しないと**、スレッド内で 1 段進むたびに購読が張り直される（`createSection` が `createEffect` の中で `source()` を読むため）
- `thread-spine.ts` は純関数でテストが速く [ADR-0029](../../adr/0029-mutation-testing-for-build-and-write.md) と同じ扱いに値するが、**範囲拡大は見送った**こと。ADR-0029 を書いた直後であり、まず現在の範囲での運用感を見る

- [ ] **Step 3: Commit**

```bash
git add docs/design/
git commit -m "docs: スレッドのスライスの記録"
```

---

## 自己レビュー

**spec 網羅:** 2 節「含む」の 6 項目に対し、根への 1 購読 = Task 7、`threadSpine` = Task 2、`threadRoot` = Task 1、スタック = Task 7、押す導線 = Task 5、`Order` の削除 = Task 3。7 節の context = Task 4、背骨の描画 = Task 6、8.2 節の e2e = Task 8。

**計画側で決めたこと（spec に無い）:** `ThreadView` に単体テストを書かない（Task 6 Step 3 に理由を明記）。`DRAG_SLOP = 4`。`filters: []` が「何も購読しない」であることは計画作成時に `query-plan.ts` を読んで確認済み（Task 7 Step 2 に根拠を記載）。

**型の一貫性:** `threadRoot` は `IdRef | undefined`（Task 1）、`threadSpine` は `ThreadSpine`（Task 2）、`useThreadNav` は `((focusId: string) => void) | undefined`（Task 4）で、Task 5〜7 の利用と一致。

**既知の粗さ:** Task 5 と Task 8 はテスト本体を実装者に書かせている（主張と捕まえる変異は明記）。既存テストの手筋に合わせる必要があり、計画側で固定すると実際の API と食い違ったときに実装者が計画へ引きずられるため。ファイル構成表の `thread-nav.ts` は `.tsx` が正しい（Task 4 Step 3 で訂正済み）。
