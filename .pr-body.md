## Summary
- Remove the stale one-off `docs/architecture/nostr-core-v1.md` document that still described TanStack DB collections as the target v1 read model.
- Update the migration plan to point PR 1 at the split `docs/nostr-client-core-design-v1/*` documents as the canonical implementation reference.
- Keep the remaining TanStack DB mentions limited to explicit pivot/history notes that say it is not the target architecture.

## Validation
- `git grep -n -i -E 'tanstack db collections|tanstack db data model|tanstack db: reactive|architecture/nostr-core-v1|docs/architecture/nostr-core-v1' -- docs || true`
- `pnpm run check`

Linear: EYE-134