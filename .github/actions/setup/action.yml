name: Setup
description: Vite+（vp）・Node・pnpm を用意し、依存を入れる
runs:
  using: composite
  steps:
    # Node は .node-version、pnpm は package.json の packageManager、vp は vite-plus の版に合わせる。
    - uses: voidzero-dev/setup-vp@v1.21.1
      with:
        node-version-file: .node-version
        cache: true
